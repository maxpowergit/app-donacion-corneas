import { StyleSheet } from 'react-native'

import { colores } from '../Base'

export default StyleSheet.create({
  contenedorPreguntas: {
    padding: 8
  },
  boton: {
    alignSelf: 'center'
  },
  textoBoton: {
    color: colores.texto
  }
})
