import { StyleSheet } from 'react-native'

import { colores } from '../Base'

export default StyleSheet.create({
  subtitulo: {
    alignSelf: 'center',
    color: colores.texto,
    padding: 20
  }
})
