import { StyleSheet } from 'react-native'

import { colores, estilos } from '../Base'

export default StyleSheet.create({
  subtitulo: {
    ...estilos.fuente,
    alignSelf: 'center',
    padding: 16
  }
})
